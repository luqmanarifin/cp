#include <bits/stdc++.h>

using namespace std;

int main() {
  int a;
  cin >> a;
  if(a > 0) {
    puts("positif");
  } else if(a == 0) {
    puts("nol");
  } else {
    puts("negatif");
  }
  return 0;
}
