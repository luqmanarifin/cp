#include <bits/stdc++.h>

using namespace std;

const int N = 45;

long long dp[N][2];

int main() {
  long long s, k;
  cin >> s >> k;
  
  dp[0][0] = 1;
  for(int i = 0; i < N; i++) {
    
  }
  
  return 0;
}
