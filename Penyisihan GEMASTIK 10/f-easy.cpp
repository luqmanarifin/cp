
#include <bits/stdc++.h>

using namespace std;

const int N = 55;
const int mod = 1e9 + 7;

char s[N];
int n, k, len;

long long

int main() {
  int t;
  scanf("%d", &t);
  while (t--) {
    scanf("%s %d %d", s, &n, &k);

  }

  return 0;
}
