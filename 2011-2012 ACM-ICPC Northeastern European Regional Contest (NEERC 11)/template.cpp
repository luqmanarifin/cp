#include <bits/stdc++.h>

using namespace std;

const string NAME = "template";

int main() {
#ifndef LOCAL
  freopen((NAME + ".in").c_str(), "r", stdin);
  freopen((NAME + ".out").c_str(), "w", stdout);
#endif
  


#ifndef LOCAL
  fclose(stdin);
  fclose(stdout);
#endif

  return 0;
}
