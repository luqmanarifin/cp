#include <bits/stdc++.h>

using namespace std;
const string filename = "A";

int main() {
#ifndef LOCAL
  freopen((filename + ".in").c_str(), "r", stdin);
  freopen((filename + ".out").c_str(), "w", stdout);
#endif

  

#ifndef LOCAL
  fclose(stdin);
  fclose(stdout); 
#endif
  return 0;
}