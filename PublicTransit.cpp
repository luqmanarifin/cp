#include <bits/stdc++.h>

using namespace std;

class PublicTransit {
public:
  int get(int i, int j) {
    int node = n * m;
    int dist[node][node];
    for(int i = 0; i < )
  }
  
  int minimumLongestDistance(int _n, int _m) {
    n = _n;
    m = _m;
    int ans = get(0, 0);
    for(int it = 0; it < n * m; i++) {
      for(int jt = it + 1; jt < n * m; jt++) {
        
      }
    }
    return ;
  }
  int n, m;
  const int inf = 1e9;
}

int main() {
  int n = 0;
  int m = 0;
  
  PublicTransit PublicTransit;
  cout << k.minimumLongestDistance(n, m) << endl;
  
  return 0;
}
