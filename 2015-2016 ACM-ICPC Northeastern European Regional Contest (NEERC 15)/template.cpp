#include <bits/stdc++.h>

using namespace std;

const string NAME = "filename";

int main() {
  freopen((NAME + ".in").c_str(), "r", stdin);
  freopen((NAME + ".out").c_str(), "w", stdout);
  
  
  
  fclose(stdin);
  fclose(stdout);
  return 0;
}
