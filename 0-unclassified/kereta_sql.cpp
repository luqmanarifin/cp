#include <bits/stdc++.h>

using namespace std;

bool iscode(string str) {
  
}

int main() {
  string buffer;
  while(getline(cin, buffer)) {
    string 
    cout << "(\"" << buffer << "\")," << endl;
  }
  
  return 0;
}
