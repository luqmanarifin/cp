#include <bits/stdc++.h>

using namespace std;

char s[20][20];

int main() {
  int n, m;
  scanf("%d %d", &n, &m);
  for(int i = 0; i < n; i++) {
    scanf("%s", s[i]);
  }
  int ans = 0;
  for(int i = 0; i < n; i++) {
    for(int j = 0; j < m; j++) {
      for(int k = 0; k < n; k++) {
        for(int l = 0; l < m; l++) {
          
        }
      }
    }
  }
  cout << ans << endl;
  return 0;
}
