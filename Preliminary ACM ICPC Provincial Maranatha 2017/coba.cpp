#include <bits/stdc++.h>

using namespace std;

int main() {
  priority_queue<pair<string, int>> pq;
  pq.emplace("aning", 0);
  pq.emplace("su", 0);
  cout << pq.top().first << endl;
  
  cout << ("d()" < "dadada()") << endl;

  return 0;
}
