#include <bits/stdc++.h>

using namespace std;

void init(string file_name) {
  freopen((file_name + ".in").c_str(), "r", stdin);
  freopen((file_name + ".out").c_str(), "w", stdout);
}

int main() {
  init("filename");
  
  return 0;
}
