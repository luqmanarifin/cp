#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, now = 0;
  scanf("%d", &n);
  for(int i = 1; ; i++) {
    printf("mb");
    for(int j = 0; j < i; j++) {
      printf("e");
    }
    now += 2 + ei;
    if(now >= n) break;
  }
  printf("\n");
  return 0;
}
