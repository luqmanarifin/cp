#include <bits/stdc++.h>

using namespace std;

const int N = 2005;

string dir = "LRDU";
int pos[256];

int dx[] = {0, 0, 1, -1};
int dy[] = {-1, 1, 0, 0};

int main() {
  for (int i = 0; i < 4; i++) {
    pos[dir[i]] = i;
  }
  int t;
  scanf("%d", &t);
  while (t--) {
    int n, m;
    scanf("%d %d %s", &n, &m, s);

  }

  return 0;
}
